<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hub.gisdatahub.board.mapper.NoticeSqlMapper">

<select id="findByNoticeList">
        SELECT * FROM sd_board_post WHERE board_type_code = 'NOTICE'
        AND deleted_yn = 'N'
        ORDER BY pinned_yn DESC, created_at DESC
</select>

</mapper>