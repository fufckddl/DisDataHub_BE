<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hub.gisdatahub.board.mapper.InquirySqlMapper">
    <!--사용자 질문 게시판 목록 조회 -->
    <select id="findInquiryList" resultType="com.hub.gisdatahub.board.dto.InquiryDetailDto">
        SELECT
            bp.post_id,
            bp.board_type_code,
            bp.user_id,
            bp.title,
            bp.content,
            bp.visibility_status,
            bp.pinned_yn,
            bp.created_at,
            bp.view_count,
            bp.updated_at,
            bp.deleted_yn,
            id.inquiry_category_code,
            id.inquiry_status_code,
            id.created_at AS inquiry_created_at,
            id.updated_at AS inquiry_updated_at
        FROM sd_board_post bp
        INNER JOIN sd_inquiry_detail id
            ON bp.post_id = id.post_id
        WHERE bp.board_type_code = 'INQUIRY'
          AND bp.deleted_yn = 'N'
          AND bp.visibility_status = 'PUBLIC'
        ORDER BY bp.created_at DESC
    </select>

    <!-- 문의 게시글 기본 정보 저장 -->
    <insert id="insertInquiryPost"
            parameterType="com.hub.gisdatahub.board.dto.BoardPostDto"
            useGeneratedKeys="true"
            keyProperty="postId"
            keyColumn="post_id">
        INSERT INTO sd_board_post (
            board_type_code,
            user_id,
            title,
            content,
            visibility_status,
            pinned_yn
        )
        VALUES (
            'INQUIRY',
            #{userId},
            #{title},
            #{content},
            #{visibilityStatus},
            'N'
        )
    </insert>

    <!-- 문의 게시글 상세 정보 저장 -->
    <insert id="insertInquiryDetail"
            parameterType="com.hub.gisdatahub.board.dto.InquiryDetailDto">
        INSERT INTO sd_inquiry_detail (
            post_id,
            inquiry_category_code,
            inquiry_status_code
        )
        VALUES (
            #{postId},
            #{inquiryCategoryCode},
            'RECEIVED'
        )
    </insert>
</mapper>
